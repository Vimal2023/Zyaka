// eslint-disable-next-line no-unused-vars
import React from 'react'

import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
          Our chefs are not just cooks; they are artists who meticulously craft every dish with a blend of expertise and passion. From the sizzle of the grill to the delicate touch in plating, each creation is an expression of their dedication to the culinary craft.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;